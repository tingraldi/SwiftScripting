public enum SafariScripting: String {
    case contentsprovider = "contentsProvider"
    case document = "document"
    case sourceprovider = "sourceProvider"
    case tab = "tab"
    case window = "window"
}
